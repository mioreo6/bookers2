

<%= render 'books/list', book: @book %>

<h2>Users</h2>
<table class="table">
    <thead>
     <tr>
      <th></th>
      <th>image</th>
      <th>name</th>
     </tr>
    </thead>

    <tbody>
        <% @users.each do |user| %>
        <tr>
        <td><%= image_tag user.get_profile_image(100,100) %></td>
        <td><%= user.name %></td>
        <td></td>
        </tr>
         <% end %>
    </tbody>


</table>
</div>